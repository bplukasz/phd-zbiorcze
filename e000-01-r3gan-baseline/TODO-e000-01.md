# TODO po wydzieleniu `e000-01-r3gan-baseline`

Dodatkowe tematy, które wyszły podczas implementacji i krótkim przeglądzie kodu.

## P0 (najpierw)

Wszystkie punkty P0 zostały zaimplementowane:

1. [x] Strict validation nieznanych kluczy (`base/profile/override`) w `src/config_loader.py`.
2. [x] Centralne `RunConfig.validate()` po merge w `ConfigLoader.get_config()`.
3. [x] Ujednolicenie do pojedynczego pola `img_resolution` (usunięte `img_size`).
4. [x] Silniejsze, typowane parsowanie `--override` w `run.py`.
5. [x] Przeniesienie `r3gan-source.py` do `src/r3gan_source.py`.

## P1 (duży zysk utrzymaniowy)

5. [x] **Podział `src/experiment.py` (train loop) na mniejsze moduły**.
   - Wydzielić: `artifact_io`, `checkpointing`, `metrics_runtime`, `eta_logging`, `training_loop`.
   - Zostawić `train()` jako cienki orchestrator.

6. [x] **Usunąć hacki `sys.path` i uporządkować importy pakietowe**.
   - Dotyczy: `run.py`, `src/__init__.py`, `conftest.py`.
   - Docelowo uruchamianie modułowe (`python -m ...`) albo instalacja editable.

7. **(DONE) Refaktor dynamicznego ładowania `r3gan-source.py`**.
   - Zastąpiono dynamiczne importy bezpośrednim importem pakietowym z `src/r3gan_source.py`.

8. [x] **Zweryfikować czy nic w e000 nie korzysta już z `img_size`**.
   - Jeśli coś koorzysta należy przepisać na `img_resolution`.

9. [x] **Dodać checkpoint resume** (`--resume` + kontynuacja logów/EMA/optimizerów).
   - Aktualnie checkpointy są zapisywane, ale brak standardowej ścieżki wznowienia treningu.

## P2 (jakość/testowalność)

10. [x] **Lepsze rozdzielenie I/O od logiki obliczeń** w `src/experiment.py`.
   - Ograniczyć `print()` i operacje plikowe wewnątrz pętli.
   - Ułatwi to testy jednostkowe i ewentualne użycie innego loggera.

11. [x] **Pokryć testami helpery i edge-case’y konfiguracji/danych**.
    - Dodać testy dla: `_estimate_remaining_seconds`, `_update_fid_auc`, `_count_remaining_metric_evals`.
    - Dodać testy loadera danych dla gałęzi `dataset_name` i błędnych wartości.

12. [x] **Dodać jawny fallback dla `--resume latest` gdy `out_dir` jest auto-generowany**.
    - Obecnie `latest` szuka checkpointów w bieżącym `out_dir/checkpoints`.
    - Przy auto `out_dir` łatwo wskazać nowy, pusty katalog; warto mieć czytelniejszy komunikat + sugestię podania ścieżki.

## P3 (drobne follow-up po P2)

13. **Dodać test integracyjny CLI dla fallbacku `--resume latest`**.
    - Sprawdzić, że `run.py` zwraca czytelny komunikat i podpowiedź `--out-dir`/`--resume <path>`.

